import styled from "styled-components"

export const StyledAuthButton = styled.button`
    //Layout
    display: inline-flex;
    padding: 4px 20px;
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
    
    //Style
    border-radius: 10px;
    border: 1px solid var(--clr-cyan-1000, #031717);
    background: #FFF;

    /* GShadow/min */
    box-shadow: 2px 2px 0 0 var(--clr-green-1000, #061407);
`
